package myPackage;

public class EmployeeView {
    public void printEmployeeDetails(String name, int salary, int bonus) {
        System.out.println("Employee`s name: " + name + ". Salary: " + salary + "$. Bonus: " + bonus +"$");
    }
}
